meta {
  name: Create Referral
  type: http
  seq: 1
}

post {
  url: {{protocol}}://{{host}}/ui/user/services/referrals
  body: none
  auth: apikey
}

headers {
  : 
}

auth:apikey {
  key: Authorization
  value: {{auth_jwt}}
  placement: header
}
