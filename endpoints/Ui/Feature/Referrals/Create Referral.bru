meta {
  name: Create Referral
  type: http
  seq: 2
}

post {
  url: {{protocol}}://{{host}}/ui/user/self/services/referrals
  body: json
  auth: apikey
}

headers {
  : 
}

auth:apikey {
  key: Authorization
  value: {{auth_jwt}}
  placement: header
}

body:json {
  {
    "label": "TEST",
    "url": "https://example.com"
  }
}
